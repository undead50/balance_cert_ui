import { useEffect } from "react";
import { useFetch } from "../../hooks";

function Dashboard() { 
    
    const content = <>
    <h1>dasboard</h1>
    </>
    return (content);
}

export default Dashboard;